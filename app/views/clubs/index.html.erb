
    <h2>Select your Book Club</h2>
    <ul>
      <% @clubs.each do |club| %>
        <li>
          <%= link_to club.name, club_path(club) %>
        </li>
      <% end %>
    </ul>

  <div class= "add">
    <%= link_to "Create New Book Club", new_club_path %>
  </div>
